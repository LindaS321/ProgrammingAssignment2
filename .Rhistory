dir()
cd ..
cd .
cd /
0
ls
ls
dir()
cd ~
d
ls
ls()
dir()
source("makeVector.R")
makeVector()
test <- makeVector()
test
m
y
x
$getmean
getmean
library(datasets)
data(iris)
colMeans(iris)
library(datasets)
data(mtcars)
tapply(mtcars$cyl, mtcars$mpg, mean)
sapply(mtcars, cyl, mean)
with(mtcars, tapply(mpg, cyl, mean))
abs(with(mtcars, tapply(mpg,cyl,mean)))
head(mtcars)
with(mtcars, tapply(hp, cyl, mean))
with(mtcars,abs(tapply(hp, cyl, mean)))
